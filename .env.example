GEMINI_API_KEY=your_gemini_key_here
GITHUB_TOKEN=your_github_token_here

# Any public GitHub repo — 'owner/repo' or a full GitHub URL
# Used by the ingestion test script and (eventually) as the default target
TEST_REPO=vercel/ms
